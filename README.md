# UNO Multiplayer (Monorepo)

This repository contains a phased implementation of a realtime multiplayer UNO game. It uses a monorepo layout with `apps/` and `packages/`.

Phases are defined in `phases.md` and implemented iteratively.

Quick start (after installing dependencies):

```bash
npm install
npm run dev
```
